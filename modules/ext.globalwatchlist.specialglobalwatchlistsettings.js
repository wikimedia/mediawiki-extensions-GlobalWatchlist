/**
 * Javascript for Special:GlobalWatchlistSettings
 */
( function () {
	'use strict';

	// On ready initialization
	$( function () {
		/* eslint-disable-next-line no-console */
		console.log( 'GlobalWatchlistSettings javascript loaded!' );
	} );
}() );